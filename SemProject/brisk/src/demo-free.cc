// Created by Reto

#include "brisk/libraries.h"                          						//Loads all the libraries needed to run this code
using namespace std;
using namespace cv;


int main()
{
  // int a = 1;
  // int nimages; 
  // string dirname;
  // dirname = "00007433";															//Choose here the directory
  // int IntensityValue = 80;													    //Choose here the gray-intensity treshold
  // double PercRemove = 0.3;														// Choose here the percentage of dark images you want to keep. For example, if more than 30% of the keypoints are darker than the treshold, that image will not be keeped in the folder
  // int removed = 0;	
												 

  // string dirName = "FilterImages/images/AMOS_Data/FD/00006469/2014.08/20140802_103030.jpg";   //Choose here the whole path to the image, this one is used for vizualizations
  // Mat image= imread(dirName);
  // reStructure(); //Converts a yml to csv
  // vizualization(image); //Run this if you want to vizualize
  // runAllDescriptors();
  // readallDirs();  // read all dirs converts all the images to gray
  // runAll(a, nimages, IntensityValue, PercRemove, removed, "cameras.txt"); //Here you only crop/resize 
  // runAllDescriptors(); // Here the descriptors of all keypoints are computed, the mean value and the most stable keypoints are computed
  // reStructure();
  // viz();
  // 
  return 0;
}